<div>

<h3><%= notice %></h3>

	<%= form_for(@topic) do |f| %>
		<%= f.text_field :content %>
		<br>
		<%= f.submit %>
	<% end %>

	<% @topics.each do |topic| %>
		<div class="row">
			<h4><%= topic.user.name %>（<%= topic.created_at %>）</h4>
			<p><%= topic.content %></p>
      <%= link_to "編集", edit_topic_path(topic.id) %>
      <%= link_to "詳細", topic_path(topic.id) %>
      <%= link_to '削除', topic_path(topic.id), method: :delete, data: {confirm: "本当に削除していいですか？?"} %>
		</div>
	<% end %>

</div>